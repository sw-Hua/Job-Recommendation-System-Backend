server:
  port: 8080
spring:
  application:
    name: JrSystem_DbTest
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5432/JrSystem_DbTest
    username: amigoscode
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true